# Deployment

- Development: local branch/workspace.
- Staging: preview-only release candidate; never aliases the production domain.
- Production: frozen until explicit user authorization.

Historical production project: `ens-sonson-app`.
Historical production deployment: `dpl_6E3zYFnG7B2U9jwjohZUohE7waTV`.

No staging action may replace that deployment or move its domain. Every staging deployment must be attributable to a Git commit.
